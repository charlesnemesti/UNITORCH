/** Minimum UNITORCH balance to claim a Torch NFT from the on-chain registry. */
export const HOLDER_THRESHOLD = 200;

/** Human-readable threshold for UI copy. */
export const HOLDER_THRESHOLD_LABEL = '200';
